# Milestone 13 Completion Record

Status: `complete`

Completed: 2026-08-09

## Delivered slices

1. Frozen versioned state model, operation catalog, oracle-admission ledger,
   two seed tiers, compact fixtures, identities, hashes, and fail-closed
   budgets.
2. Two legal real-Qt generated sequences reaching exact authoritative terminal
   boundaries.
3. Four illegal/recovery real-Qt sequences proving typed rejection, immediate
   no mutation/no dispatch, state continuity, same-lineage recovery, and exact
   terminal authority.
4. Schema-v2 fresh-process aggregation, original normalized-sequence replay,
   semantic coverage, immutable original-failure retention, and diagnostic
   isolation. Reduction remains deliberately disabled.
5. Frozen qualification, required Windows-visible representatives,
   deterministic compatibility, regressions, operator documentation, and
   closeout.

## Qualified result

`design_calibration_lifecycle_v1` freezes seeds `13, 29, 47, 83, 131, 197`.
Its passing frozen campaign completely covers 12 states, 34 transitions, 26
oracle-admitted operations, and eight rejection classes. Every sequence stays
within the `18/80` semantic/action cap, three sessions/two rotations, four
screenshots, 256 files/48 MiB, 270/300-second bounds, and compact `4/2/8/44`
reaction/stock/intent/drop workload. Aggregate limits are `108/480`, 18/12,
24 screenshots, 1,600 files/320 MiB, and 1,800 seconds.

Frozen direct, aggregate, exact original replay, all four required visible
direct/replay representatives, and diagnostic isolation passed. Deterministic
Milestones 9-12, Milestone 8 compatibility, lifecycle, and host-regression
passed direct/replay. The immutable optimizer-360 positive control passed with
unchanged hashes, three sessions, revisions 1-8, 1,800 intents, and 46,208
droplets. Focused tests passed 356/356, selected real-Qt/system tests passed 66
with three expected skips, and the exact default suite passed 4,290 with 134
skips.

The authoritative detailed evidence, hashes, commands, limits, exclusions,
risks, and rollback are recorded in
`docs/sil_interactive_simulation_milestone_13_slice_5_completion.md`; slice
implementation and completion records remain independently reviewable.

## Resolved decisions

No implementation-blocking decision remains. Coverage/budget review retained
the proposed seeds `13, 29, 47, 83, 131, 197`; Milestone 8 seeds were not
automatically promoted. Both compact UI-dependent operations
`array.start_while_active_via_ui` and
`head.attempt_unsafe_exchange_via_ui` proved deterministic and stayed admitted.
Generated persistence faults, refill/resume, a reducer, manifest capability
registration, and automatic diagnostic promotion remain intentionally excluded
rather than unresolved.

## Remaining exclusions

- No refill-required/resume operation while volume tracking is disabled and
  `execution.refill_resume` remains deferred.
- No active-authority mutation outside the existing isolated Milestone 12
  fixture.
- No unseeded acceptance, random walk, unattended scheduling, `pi_stress`,
  firmware/protocol/hardware change, or application-SIL hardware claim.
- The known `print_array_stress_384x10_v1` mismatch remains separately scoped;
  complete `host_stress` is not a Milestone 13 gate.
- Diagnostic seeds remain non-gating until deliberately reviewed and promoted.

## Rollback

Remove only the additive Milestone 13 campaign, runner/selector branches,
journey/assertion helpers, tests, and documentation. Retain Milestone 8,
deterministic Milestones 9-12 and 11A, historical evidence, and all user data.
