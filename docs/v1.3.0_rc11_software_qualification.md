# v1.3.0-rc.11 Legacy Bridge Software Qualification

Date: 2026-08-27

Status: merged-main software qualification passes. Exact release qualification
remains pending for the local rc.11 tag. Pi/physical qualification is also
pending.

## Qualified scope

- Direct legacy sources: `v1.2.0-rc.6`, `v1.2.0`, and `v1.3.0-rc.1`.
- Exact bridge target: `v1.3.0-rc.11`.
- Case-insensitive `LabCraftUpdates/` remnants are backup-only and do not enter
  canonical configuration.
- The target release must opt in through
  `labcraft_update_compatibility_v1`; missing, malformed, undeclared, or hidden
  extra source authority fails closed.
- The exact legacy pointer remains on rc.11 while modern series discovery can
  advance to manifest-schema-v2 release candidates.
- Historical rc.2/rc.3/rc.8 genesis behavior remains unchanged.
- Offline bundle creation, discovery, and operator workflow are out of scope.
- Firmware, device protocol, hardware composition, and Pi rehearsal tooling are
  unchanged.

## Software evidence

The focused command used a unique external pytest base directory and passed:

```powershell
.\env\Scripts\python.exe -m pytest -q `
  tests\test_machine_data_ownership.py `
  tests\test_machine_data_bootstrap.py `
  tests\test_machine_data_deployment_anchor.py `
  tests\test_machine_data_update_preservation.py `
  tests\test_update_and_restart.py `
  tests\test_validate_release_metadata.py `
  tests\test_legacy_bridge_software_qualification.py `
  --basetemp=C:\Users\conar\AppData\Local\Temp\labcraft_rc11_merged_focused_rerun_20260827
```

Result: `216 passed in 39.75s`.

The isolated legacy campaign creates a new temporary author repository, bare
origin, and machine clone for each source cohort. It executes the immutable
source tag's updater without `--target-release`. The origin advertises rc.11 as
the exact pointer while both rc.11 and a synthetic manifest-schema-v2 rc.12 tag
exist. All three cohorts selected the exact rc.11 commit. Ignored bundle
remnants remained byte-for-byte unchanged and each checkout remained clean.

Additional gates:

- release metadata validation: passed;
- strict parsing of every `releases/*.json` file: passed;
- Python compilation of affected modules and qualification tests: passed;
- `git diff --check`: passed, with only Git's existing LF-to-CRLF working-copy
  notices for two test files.

## Repaired example-design fixture

The missing checked-in example input was restored at
`FreeRTOS-interface/Experiments/CSV_upload_examples/sample_target_concentrations.csv`.
It contains 103 explicit wells and six independent 64-level concentration
columns. This preserves the sparse uploaded-design condition exercised by the
size guard without changing application logic.

The two formerly failing tests pass directly, and both complete adjacent test
modules pass:

```text
2 passed in 2.51s
73 passed in 4.22s
```

## Full-suite result

The full Windows suite was rerun with a new unique external `--basetemp`:

```powershell
.\env\Scripts\python.exe -m pytest -q `
  --basetemp=C:\Users\conar\AppData\Local\Temp\labcraft_rc11_main_full_20260827
```

Result: `5,872 passed, 158 skipped, 541 warnings in 525.19s (0:08:45)`.

## Exact-tag and rollout gates still required

The implementation is merged into local `main`, rc.11 metadata is prepared, and
the rc.10 firmware artifact identity is verified. Remaining gates:

1. Commit the rc.11 metadata with manifest schema v1, the exact bridge
   declaration, pinned pointer, and three exact legacy sources.
2. Create the annotated rc.11 tag locally and rerun the entire isolated campaign
   against that exact tag.
3. Push `main` and rc.11 only with explicit release authorization.
4. Use the first available machine as an attended canary. Record Pi/physical
   qualification separately before broader rollout.

If an unpushed local tag fails qualification, delete only that local tag, fix
the implementation, and recreate it. Never retarget a pushed release tag.
